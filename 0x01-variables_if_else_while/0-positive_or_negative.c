#include <stdio.h>

/**
 * main - this is the main code
 *
 *
 *
 * Return: Always 0
 */

int main(void)
{
	int (n) = 0;

	if (n > 0)
	{
		printf("98 is positive\n");
	}
	else if (n == 0)
	{
		printf("0 is zero\n");
	}
	else if (n < 0)
	{
		printf("-98 is negative\n");
	}
	return (0);
}
