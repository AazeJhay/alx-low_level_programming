#include <stdio.h>

/**
 * main - this is the main code
 *
 *
 *
 * Return: Always 0
 */

int main(void)
{
	int (n) = 0;

	if (n > 0)
	{
		printf("is positive\n");
	}
	if (n == 0)
	{
		printf("is zero\n");
	}
	if (n < 0)
	{
		printf("is negative\n");
	}
	return (0);
}
