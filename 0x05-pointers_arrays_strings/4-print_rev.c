#include "main.h"
/**
 * print_rev - prints in reverse
 * @s: the string
 * Return: 0
 */
void print_rev(char *s)
{
	int j = 0;
	int x;

	while (s[j] != '\0')
		j++;
	for (a = b - 1; a >= 0; 1--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
